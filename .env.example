# PostHog analytics (see posthog-setup-report.md)
# Public project token — safe to expose to the client. Events are sent
# through the /ingest reverse proxy configured in next.config.ts.
NEXT_PUBLIC_POSTHOG_PROJECT_TOKEN=phc_your_project_token
